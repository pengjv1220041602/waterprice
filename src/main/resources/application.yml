server:
  port: 80
spring:
  application.name: iebook
  datasource:
     url: jdbc:mysql://localhost:3306/waterprice?useUnicode=true&characterEncoding=utf-8
     username: root
     password: root
     driver-class-name: com.mysql.jdbc.Driver
  thymeleaf:
     mode: HTML
     cache: false
     prefix: classpath:/templates

# 分页插件
pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql